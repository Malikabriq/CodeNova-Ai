from agent.graph.state import State

def planner_node(state: State):
    """
    Converts user request into a step-by-step plan and updates the state.
    """
    print("---PLANNING---")
    # Implementation for LLM prompt template and JSON plan return
    return state
